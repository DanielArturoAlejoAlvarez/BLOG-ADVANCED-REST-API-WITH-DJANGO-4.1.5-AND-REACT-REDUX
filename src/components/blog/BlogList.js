import { connect } from "react-redux"


function BlogList() {
  return (
    <div>BlogList</div>
  )
}



const mapStateToProps = state=>({

})

export default connect(mapStateToProps, {
    
})(BlogList)